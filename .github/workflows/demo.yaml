name: Create File Example

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Create a file
      run: |
        echo "Hello, World!" > ${{ github.workspace }}/log.txt
        cat ${{ github.workspace }}/log.txt # Optional: Verify the file content